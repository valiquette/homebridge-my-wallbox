export const PLATFORM_NAME = 'wallbox'; // This is the name of the platform that users will use to register the plugin in the Homebridge config.json
export const PLUGIN_NAME = 'homebridge-my-wallbox'; //This must match the name of your plugin as defined the package.json
export const PLUGIN_VERSION = '0.1.25'; //version